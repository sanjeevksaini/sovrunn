// Package apivalid defines strict decode and layered validation grammar
// for API request bodies and resource fields, including decode helpers,
// field policies, finite limits, concurrency checks, and the ordered
// validation pipeline. It may import apimeta, apiref, apicond, and
// apiproblem, and must not import apischema.
package apivalid
